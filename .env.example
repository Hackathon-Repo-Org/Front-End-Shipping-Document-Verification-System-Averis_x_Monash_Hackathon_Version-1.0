# The backend origin. REQUIRED — ui/ hardcodes no hostname anywhere.
# Local development against a locally running API:
VITE_API_BASE_URL=http://localhost:8000
# Deployed (Azure Container Apps), for reference:
# VITE_API_BASE_URL=https://shipdoc-api.<region>.azurecontainerapps.io
